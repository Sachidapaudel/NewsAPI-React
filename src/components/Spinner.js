import loading from './loading.gif'; // Adjust path as necessary
const Spinner = () => {
  
  
    return (
      <div className='text-center'>
        <img src={loading} alt = "loading"/>
      </div>
    )
  
}

export default Spinner
